package spring.week1;

public class DAY4 {
/**
 * String 클래스
 * String 메소드
 * 문자열의 추출, 비교, 찾기, 분리, 변환등과 같은 다양한 메소드를 가짐
 * 사용빈도 높은 메소드
 * charAt(int index), equals(Object object), getBytes(),
 * toLowerCase(), toUpperCase(), replace("자바", "파이썬"),
 * substring(), trim() : 앞뒤 공백 잘라내기
 * valueOf() : 기본 타입의 값을 문자열로 변환
 * split() : 정규 표현식을 구분자로 해서 부분 문자열 분리
 * 배열에 저장 후 리턴
 */

/**
 * StringBuffer, StringBuilder 클래스
 * 문자열 결합 연산자 +
 * String은 내부의 문자열 수정 불가 -> 대치된 새로운 문자열 리턴
 */
}
